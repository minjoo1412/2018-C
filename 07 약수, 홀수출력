#include <stdio.h>

int main() {
	/*
	int num;
	printf("숫자를 입력하십시오");
	scanf_s("%d", &num);
	printf("%d의 약수는 : ", num);

	for (int i = 1; i <= num; i++) {
		if (num % i == 0) {
			printf("%d ", i);
		}
	}
	printf("입니다.\n");

	int x;
	int y;
	printf("insert number");
	scanf_s("%d", &x);

	y = (x % 10);
	printf("x의 1의자리 숫자는 : %d", y);
	printf("\n");
	*/

	int num2;
	printf("insert number : ");
	scanf_s("%d", &num2);
	for (int i = 1; i <= num2; i++) {
		for (int j = 1; j <= i; j++) {
			printf("%d", (2 * j) - 1);
		}printf("\n");
	}

	for (int i = 1; i <= num2; i++) {
		for (int j = 1; j <= i; j++) {
			if (j == 1) {
				printf("1 ");
			}
			else {
				printf("%d %d ", (4 * j) - 1, (4 * j) + 1);
			}
		}printf("\n");
	}
	//1  135  13579  135791113
	
}
